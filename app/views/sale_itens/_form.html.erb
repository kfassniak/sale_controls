<%= form_for(@sale_iten) do |f| %>
  <% if @sale_iten.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale_iten.errors.count, "error") %> prohibited this sale_iten from being saved:</h2>

      <ul>
      <% @sale_iten.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
